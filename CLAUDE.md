# CLAUDE.md

## 입력 구체화

모든 사용자 입력에 대해 응답 전에 항상 수행한다.

구체화된 내용을 위아래 수평선으로 감싸 출력한다:

```
---
[구체화된 완성 프롬프트]

---
```

**원칙**
- 사용자가 실제로 말하는 문장 그대로 재구성한다 — 행동 묘사·서술·요약 X - 임의 판단 금지
  - 잘못된 예: "나는 버튼 색을 바꿔달라고 요청하고 있다"
  - 올바른 예: "전송 버튼 색상을 파란색으로 바꿔줘"
- 빠진 주어·목적어·조건을 보완한다.
- 모든 과정에서 의도를 변형하지 않도록 주의한다.
- 의문형 입력은 특별한 맥락이 없으면 질문으로 간주하며, 이를 유지하도록 한다.

**추론 불가** 추론 불가한 부분은 `(?)`로 표시한다. 
- `(?)`가 있으면 구체화 블록 아래에 해당 부분을 사용자에게 언급하며 보완을 요청한다.

**피드백 상황** AI가 의도에 어긋난 결과를 낸 후 피드백이 온 경우,  
피드백만 구체화하지 않는다. 최초 프롬프트(유효 부분) + 피드백(수정 부분)을 합산해 재구성한다.
