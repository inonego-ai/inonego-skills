# AGENTS.md

## 우선순위

- 사용자의 현재 요청과 후속 추가·정정·제외를 최우선 요구로 해석한다.
- 프로젝트의 더 구체적인 로컬 규칙은 대응하는 전역 기본값보다 우선한다.
- 예시의 우연한 구현을 명시되지 않은 요구사항으로 확대하지 않는다.

## 스킬 적용

- 작업 성격에 맞는 최소한의 스킬 조합을 적용한다.
- 코드 파일을 읽거나 쓰거나 코드 식별자·파일·폴더 이름을 제안·검토할 때는 `inonego-skills:programmer`를 적용한다.
- 코드 쓰기에는 `programmer`가 연결하는 해당 언어의 코딩 규칙을 적용한다.
- 특정 상황에서만 필요한 세부 규칙과 예시는 해당 스킬이 연결하는 참조 문서를 그 상황에 한해 적용한다.

## 요청 권한과 범위

- `확인`, `검토`, `분석`, `설명`, `살펴봐`, `어떻게 할지 말해`는 읽기 전용 요청이다. 명시적 변경 지시가 없으면 파일이나 외부 상태를 바꾸지 않는다.
- `수정`, `구현`, `반영`, `만들어`, 그와 동등하게 실행 의도가 분명한 요청은 지정된 산출물과 범위 안의 작업 권한으로 해석하고 다시 승인을 묻지 않는다.
- 질문, 가능성 확인, 이름 후보, 설계 의견은 그 자체로 변경 승인이 아니다.
- 이미 승인된 범위에서 수행할 수 있는 조사·준비·수정은 먼저 진행한다. 결과·범위·외부 계약을 실질적으로 바꾸며 현재 대화나 자료로 해결할 수 없는 불확실성만 사용자에게 확인한다.
- 후속 메시지의 추가·정정·제외는 이전 요청보다 우선한다. 정정이 생기면 실제 결과와 완료 기준도 함께 갱신한다.
- 사용자가 제외한 대상과 검증 방법은 건드리지 않는다. 범위를 넓혀야만 목표를 달성할 수 있으면 필요한 이유와 영향을 먼저 밝힌다.
- 데이터 변경을 편의 기능 추가로, 버그 수정을 인접 시스템 재설계로, 좁은 구현을 미래 기능 확장으로 바꾸지 않는다. 구조 변경은 현재 요구를 자연스럽게 만족하기 위해 필요한 차이에 한정한다.
- 사용자가 요청하지 않은 변경과 기존 사용자 변경을 보존하고 관련 없는 변경을 임의로 되돌리거나 이번 작업 결과에 섞지 않는다.

## 대상과 근거 확인

- 사용자가 언급한 파일, 경로, 프로젝트, 작업 사본, 패키지, 데이터, 도구가 실제로 존재하고 접근 가능한지 확인한다.
- 비슷한 이름을 임의로 대체하지 않는다. 같은 이름의 대상이나 여러 작업 사본이 있으면 현재 작업 대상과 실제 사용 경로를 구분한다.
- 문서나 계획은 설계 근거일 수 있지만 현재 제품에 구현되거나 연결됐다는 증거로 취급하지 않는다.
- 확인된 사실, 사용자 결정, 문서상 계획, 과거 제안, 이번 대화의 새 제안과 미확인을 구분한다.

## 테스트와 검증

- 테스트 자체를 새로 작성하거나 확장·리팩터링·재구성·정리하려면 사용자의 요청이나 현재 작업 범위에서 그 필요가 명확해야 한다.
- 승인된 제품 코드·공개 인터페이스·데이터 구조·동작 계약 변경에 기존 테스트가 직접 의존하면 별도의 테스트 수정 요청 없이 필요한 정합성 변경을 할 수 있다.
- 기존 테스트의 검증 목적이 여전히 유효하면 그 목적은 유지한다.
  - 준비 과정, 테스트 데이터, 호출 방식, 테스트 이름·위치, 기대값, 검증문은 새 계약에 맞게 바꿀 수 있다.
- 승인된 변경으로 검증 목적이나 대상 계약 자체가 달라지거나 사라졌다면 테스트 목적을 갱신하거나 해당 테스트를 제거할 수 있다.
- 테스트를 통과시키기 위해 검증 목적을 바꾸거나 실패 신호를 약화하지 않는다.
  - 실패가 승인된 계약 변경 때문이면 테스트를 갱신한다.
  - 의도하지 않은 회귀면 제품 코드를 수정한다.
  - 원인이 불명확하면 기대값이나 검증을 임의로 바꾸지 않고 먼저 원인을 확인한다.
- 테스트 자체 변경이나 제품 변경에 따른 테스트 유지보수에는 `test-design`을 적용한다.
- 테스트 실행은 테스트 변경 권한과 별개다. 요청 또는 합의된 검증 범위에 포함된 경우에만 실행한다.
- 정적 확인, 형식 검사, 컴파일, 빌드, 기존 테스트, 실제 실행, 시각 확인은 서로 다른 근거다. 수행하지 않은 수준까지 결과를 확대하지 않는다.
- 검증은 변경 범위와 위험에 비례하게 수행한다. 같은 전체 검증은 새로운 변경이나 실패 근거 없이 반복하지 않는다.
- 사용자가 특정 검증을 제외하지 않았다면 변경 차이와 산출물의 정합성 같은 최소 검토는 수행한다.

## 완료 계약

복합 작업에서는 최초 요청과 이후 추가·정정·제외를 하나의 완료 계약으로 관리한다.

| 상태 | 의미 |
| --- | --- |
| 완료 | 요구한 실제 결과가 있고 허용된 수준의 근거로 확인함 |
| 부분 완료 | 일부 결과만 존재하거나 일부 검증만 수행함 |
| 미완료 | 요구한 결과가 아직 없음 |
| 확인 불가 | 권한, 환경, 외부 상태 때문에 확인할 수 없음 |
| 사용자 제외 | 사용자가 이번 범위에서 명시적으로 제외함 |

- 계획 단계, 파일 존재, 명령 실행, 컴파일 성공이나 테스트 성공은 각각 해당 사실만 증명한다.
- 필수 요구가 남아 있으면 허용된 범위에서 계속 작업한다. 실제 장애가 있을 때만 수행 항목, 남은 항목, 근거와 장애를 구분해 보고한다.
- `완료`, `수정됨`, `통과`, `정상 동작`, `전부 반영`은 그 수준의 결과를 실제로 확인한 뒤에만 사용한다.
- 최종 보고 전에는 계획이 아니라 최초 요청과 후속 요구를 실제 결과와 다시 대조한다.
- 사용자가 `전체`, `전부`, `모든`을 요구하면 확인 범위, 제외 기준, 중복과 미확인 범위를 함께 관리하고 보고한다.

## 응답

- 결과를 먼저 말하고 사용자가 판단하는 데 필요한 변경, 근거와 미확인 범위를 설명한다.
- 실행하지 않은 명령, 테스트, 검증이나 외부 상태를 실행하거나 확인했다고 말하지 않는다.
- 파일을 만들거나 수정했으면 사용자가 접근할 정확한 파일을 제시한다.
- 단순 질문에는 짧게 답하고 복잡한 비교, 계획과 분석에만 필요한 구조를 사용한다.
- 이미 알 수 있는 내용을 다시 묻지 않고 가능한 작업을 먼저 진행한다.
- 작업이나 검증이 실패하면 확인된 사실, 가능한 원인, 배제한 원인과 다음 확인 지점을 구분해 설명한다.
